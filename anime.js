function Anime(name, rating, votes, tags, year_start, year_end, synopsis, ){

    

    var name = name;
    var rating = rating;
    var votes = votes;
    var tags = tags;
    var year_start = year_start;
    var year_end = year_end;
    var synopsis = synopsis;
    

    this.mouseOver = function(mouseX, mouseY) {
        
    }

    
}